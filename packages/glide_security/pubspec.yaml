name: glide_security
description: Pairing, session tokens, redaction and network policy for Glide.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  crypto: ^3.0.3
  glide_protocol: ^0.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0

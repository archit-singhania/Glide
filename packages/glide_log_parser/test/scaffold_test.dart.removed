import 'dart:io';

import 'package:test/test.dart';

// Placeholder that keeps `dart test` green until this package gains code.
// Delete it when the package's first real test lands.
void main() {
  test('package is scaffolded', () {
    expect(File('pubspec.yaml').existsSync(), isTrue);
  });
}

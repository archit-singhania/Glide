name: glide_flutter_bridge
description: Orchestrates the official Flutter tool (daemon and run --machine) for Glide.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  glide_protocol: ^0.1.0
  path: ^1.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
